{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npx lerna run dev --scope=server",
    "dev:client": "npx lerna run dev --scope=client",
    "build:client": "npx lerna run build --scope=client",
    "dev": "lerna run dev --parallel --scope=server --scope=client"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
