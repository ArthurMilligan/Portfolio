version: "3.9"

services:
  server:
    container_name: server
    image: server
    build:
      context: ./packages/server/
      dockerfile: Dockerfile.server
    restart: always
    depends_on:
      - client
    environment:
      - "POSTGRES_USER=${POSTGRES_USER}"
      - "POSTGRES_PASSWORD=${POSTGRES_PASSWORD}"
      - "POSTGRES_DB=${POSTGRES_DB}"
      - "POSTGRES_HOST=${POSTGRES_HOST}"
      - "POSTGRES_PORT=${POSTGRES_PORT}"
      - "SERVER_PORT=${SERVER_PORT}"
    ports:
      - '3100:3100'
    networks:
      - portfolio_ibrae

  client:
    container_name: client
    image: client
    build:
      context: ./packages/client/
      dockerfile: Dockerfile.client
    volumes:
      - dist:/app/dist

  db:
    container_name: portfolio_db
    image: postgres
    ports:
      - "3012:5432"
    restart: always
    environment:
      - "POSTGRES_USER=${POSTGRES_USER}"
      - "POSTGRES_PASSWORD=${POSTGRES_PASSWORD}"
      - "POSTGRES_DB=${POSTGRES_DB}"
    networks:
      - portfolio_ibrae


  nginx:
    container_name: nginx
    image: nginx
    restart: always
    depends_on:
      - server
    ports:
      - "90:90"
      - "444:444"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - dist:/data/www
      - etc-letsencrypt:/etc/letsencrypt
    networks:
      - portfolio_ibrae

  # certbot:
  #   image: certbot/certbot
  #   depends_on:
  #     - nginx
  #   container_name: certbot
  #   networks:
  #     - portfolio_ibrae
  #   volumes:
  #     - etc-letsencrypt:/etc/letsencrypt
  #     - dist:/data/www
  #   command:  certonly --webroot --webroot-path=/data/www --email ypypyt@gmail.com --agree-tos --no-eff-email -d ab-portfolio.ru -d www.ab-portfolio.ru

volumes:
  etc-letsencrypt:
  www-html:
  dist:

networks:
  portfolio_ibrae:
    name: portfolio_ibrae
