import { DesktopElements } from '@entities';
import { OpenModalButton } from '@features';
import { type FC } from 'react';

const Desktop: FC = () => <DesktopElements ElementButton={OpenModalButton} />;

export default Desktop;
